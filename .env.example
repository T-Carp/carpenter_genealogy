# Anthropic API Configuration
ANTHROPIC_API_KEY=your_api_key_here

# Model Configuration
CLAUDE_MODEL=claude-3-5-sonnet-20240620

# Vector Database Configuration
VECTOR_DB_PATH=./data/vector_store
VECTOR_DB_COLLECTION=genealogy_narratives

# Structured Database Configuration
STRUCTURED_DB_PATH=./data/structured_db/genealogy.db

# Neo4j Configuration (if using knowledge graph)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Application Settings
# Optimized for lineage tracing across entire book (early ancestors → modern descendants)
MAX_CONTEXT_CHUNKS=25  # Increased from 10 - captures more generations
CHUNK_SIZE=2000  # Increased from 1000 - keeps family groups together
CHUNK_OVERLAP=400  # Increased from 200 - preserves genealogical connections
CONFIDENCE_THRESHOLD=0.6  # Lowered from 0.7 - includes more possible connections for exploratory queries
